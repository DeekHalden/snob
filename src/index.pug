<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Effective JavaScript: Frogger</title>
    <link rel="stylesheet" href="css/bundle.min.css">
</head>
body.body
    .slider-container
        //- (style="display:none;")
        .range-slider
            .range-slider__anchors
                -for(var i=0; i < 4; i++)
                    img(src="./images/diamond-anchor.png").range-slider__anchor

            input(type="text" id="controls" name="controls" value="").range-slider__range
        .slide.slide--0
            H2.slide__teaser We are not snobs. We are just better than everyone else. Be our guest.
            p.slide__text
        .slide.slide--1
            H2.slide__teaser We are not snobs. We are just better than everyone else. Be our guest.
            p.slide__text
        .slide.slide--2
            H2.slide__teaser We are not snobs. We are just better than everyone else. Be our guest.
            p.slide__text 
        script(src="https://code.jquery.com/jquery-2.2.4.min.js" integrity="sha256-BbhdlvQf/xTY9gja0Dq3HiwQF8LaCRTXxZKRutelT44=" crossorigin="anonymous")
        script(src="./scripts/ion.rangeSlider.min.js")
        script(src="./scripts/pixi.min.js")
        script(src="./scripts/type.min.js")
        script(src="./scripts/probe.js")
</body>
</html>
